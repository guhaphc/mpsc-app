"use client";
import {useState} from "react";import {useRouter} from "next/navigation";import {createClient} from "@/lib/supabase/client";
export default function Login(){const [identifier,setIdentifier]=useState(""),[password,setPassword]=useState(""),[show,setShow]=useState(false),[error,setError]=useState(""),[busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setError("");setBusy(true);const supabase=createClient();const {data,emailError}=await (async()=>{const r=await supabase.rpc("resolve_login_identifier",{identifier});if(r.error)return {data:null,emailError:r.error};return {data:r.data,emailError:null}})();if(emailError||!data){setError("Username or mobile number not found.");setBusy(false);return}const {error}=await supabase.auth.signInWithPassword({email:data,password});if(error){setError("Invalid login details or account is not active.");setBusy(false);return}router.push("/dashboard");router.refresh()}return <div className="authWrap"><div className="card"><div className="hero"><div className="brand" style={{justifyContent:"center"}}><div className="brandMark">MPSC</div></div><h1>MPSC ALL-IN-ONE</h1><div className="muted">Complete MPSC Preparation Platform</div></div><form className="form" onSubmit={submit}><div><label className="label">Username or Mobile</label><input className="input" value={identifier} onChange={e=>setIdentifier(e.target.value)} required/></div><div><label className="label">Password</label><div style={{display:"flex",gap:8}}><input className="input" type={show?"text":"password"} value={password} onChange={e=>setPassword(e.target.value)} required/><button type="button" className="btn secondary" onClick={()=>setShow(!show)}>{show?"Hide":"Show"}</button></div></div>{error&&<div className="error">{error}</div>}<button className="btn primary" disabled={busy}>{busy?"Logging in…":"LOGIN"}</button><button type="button" className="btn secondary" onClick={()=>router.push("/register")}>CREATE ACCOUNT</button></form></div></div>}
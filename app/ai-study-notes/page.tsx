import {redirect} from "next/navigation";import Link from "next/link";import {createClient} from "@/lib/supabase/server";
export default async function AIStudyNotes(){const s=await createClient();const {data}=await s.auth.getClaims();if(!data?.claims)redirect("/login");const {data:p}=await s.from("profiles").select("id,role,account_status").eq("id",data.claims.sub).single();if(!p||p.role!=="student"||p.account_status!=="active")redirect("/dashboard");const {data:subjects}=await s.from("study_subjects").select("id,subject_name,stage,paper").eq("content_area","ai_notes").eq("status","published").order("subject_name");return <div className="shell"><header className="topbar"><div className="topbarBrand"><img src="/mpsc-logo.png" className="brandLogo dashboardLogo" alt="MPSC ALL-IN-ONE"/><div className="brandSub">AI STUDY NOTES</div></div><Link className="btn secondary" href="/dashboard">Back</Link></header><main className="main"><section className="dashboardHero"><div>🤖 PUBLISHED AI NOTES</div><h1>AI Study Notes</h1><p className="muted">Teacher-reviewed notes prepared from the approved Master Source.</p></section>{!subjects?.length?<section className="card"><h2>No published notes yet</h2><p className="muted">Published AI notes will appear here when released.</p></section>:<section className="card"><h2>Published Subjects</h2><div className="topicList">{subjects.map(x=><Link key={x.id} href={"/ai-study-notes/"+x.id} className="topicRow" style={{textDecoration:"none",color:"inherit"}}><div><strong>{x.subject_name}</strong><div className="muted" style={{fontSize:12,marginTop:5}}>{x.stage} · {x.paper}</div></div><span>›</span></Link>)}</div></section>}</main></div>;}
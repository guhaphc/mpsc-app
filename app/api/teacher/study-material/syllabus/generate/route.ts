import {NextResponse} from "next/server";import {GoogleGenAI,createPartFromUri,createUserContent} from "@google/genai";import {createClient} from "@/lib/supabase/server";
export const runtime="nodejs";export const maxDuration=300;
function blocks(v:any){if(!Array.isArray(v))return [];return v.map((b:any)=>{const t=["heading","subheading","paragraph","bullet","numbered","callout","table"].includes(String(b?.type))?String(b.type):"paragraph";if(t==="table")return {type:t,columns:Array.isArray(b.columns)?b.columns.map(String):[],rows:Array.isArray(b.rows)?b.rows.map((r:any)=>Array.isArray(r)?r.map(String):[]):[]};if(t==="bullet"||t==="numbered")return {type:t,items:Array.isArray(b.items)?b.items.map(String).filter(Boolean):[]};return {type:t,text:String(b?.text||"").trim()};}).filter((b:any)=>b.type==="table"?b.columns.length&&b.rows.length:(b.type==="bullet"||b.type==="numbered")?b.items.length:b.text);}
async function upload(ai:any,blob:Blob,name:string){const u=await ai.files.upload({file:blob,config:{displayName:name,mimeType:"application/pdf"}});let x=u;for(let i=0;i<60&&String(x.state)==="PROCESSING";i++){await new Promise(r=>setTimeout(r,1500));x=await ai.files.get({name:u.name!});}if(String(x.state)==="FAILED"||!x.uri||!x.mimeType)throw new Error("Gemini could not process the syllabus source.");return x;}
export async function POST(req:Request){try{const s=await createClient();const {data}=await s.auth.getClaims();if(!data?.claims)return NextResponse.json({error:"Please login again."},{status:401});const {data:p}=await s.from("profiles").select("id,role,account_status").eq("id",data.claims.sub).single();if(!p||p.role!=="teacher"||p.account_status!=="active")return NextResponse.json({error:"Only active teachers can generate notes."},{status:403});const key=process.env.GEMINI_API_KEY;if(!key)return NextResponse.json({error:"AI service is not configured."},{status:503});const f=await req.json();const nodeId=String(f.nodeId||""),mode=String(f.mode||"generate");if(!nodeId)return NextResponse.json({error:"Syllabus node is required."},{status:400});const {data:node,error:ne}=await s.from("ai_study_syllabus_nodes").select("id,title,parent_id,depth,source_page,source_id,is_leaf").eq("id",nodeId).single();if(ne||!node||!node.is_leaf)return NextResponse.json({error:"Only a final syllabus topic can have a note."},{status:400});const {data:src,error:se}=await s.from("ai_study_syllabus_sources").select("id,name,storage_path,exam,academic_year").eq("id",node.source_id).single();if(se||!src?.storage_path)throw new Error("Syllabus source PDF is not available.");const {data:signed}=await s.storage.from("ai-study-syllabus").createSignedUrl(src.storage_path,3600);if(!signed?.signedUrl)throw new Error("Could not access syllabus source.");const rr=await fetch(signed.signedUrl);if(!rr.ok)throw new Error("Could not download syllabus source.");const path:any[]=[];let cur:any=node;while(cur){path.unshift(cur.title);if(!cur.parent_id)break;const {data:par}=await s.from("ai_study_syllabus_nodes").select("id,title,parent_id").eq("id",cur.parent_id).single();cur=par;}const model=process.env.GEMINI_NOTES_MODEL||"gemini-3.5-flash-lite";const ai=new GoogleGenAI({apiKey:key});const ref=await upload(ai,await rr.blob(),src.name);const prompt="Create a comprehensive source-grounded study note for ONE exact leaf topic. Syllabus path: "+path.join(" > ")+"\nLeaf topic: "+node.title+"\nUse the uploaded PDF as the primary source. This is NOT a summary task. Preserve all source-supported details relevant to this exact topic: definitions, chronology, names, examples, classifications, causes/effects, arguments, data, tables and conclusions. Do not invent facts. Use headings, subheadings, paragraphs, genuine lists, tables and callouts semantically. If the PDF does not support a detail, do not add it from outside knowledge. Return ONLY JSON: {\"title\":\"...\",\"overview\":\"...\",\"content_blocks\":[...]}.";
const r=await ai.models.generateContent({model,contents:createUserContent([createPartFromUri(ref.uri,ref.mimeType),{text:prompt}]),config:{responseMimeType:"application/json",maxOutputTokens:50000}});let parsed:any;try{parsed=JSON.parse((r.text||"").trim())}catch{throw new Error("AI returned invalid note JSON.");}const {data:last}=await s.from("ai_study_note_versions").select("version_no").eq("node_id",nodeId).order("version_no",{ascending:false}).limit(1).maybeSingle();const version=Number(last?.version_no||0)+1;const {data:saved,error:saveErr}=await s.from("ai_study_note_versions").insert({node_id:nodeId,version_no:version,status:"draft",title:String(parsed.title||node.title),overview:String(parsed.overview||""),content_blocks:blocks(parsed.content_blocks),generation_type:mode==="regenerate"?"regenerate":"generate",ai_model:model,created_by:p.id,source_context:{source:src.name,path,page:node.source_page}}).select("id,node_id,version_no,status,title,overview,content_blocks,created_at").single();if(saveErr||!saved)throw new Error(saveErr?.message||"Could not save note draft.");const u:any=(r as any).usageMetadata||{};await s.from("ai_study_generation_logs").insert({node_id:nodeId,note_version_id:saved.id,user_id:p.id,operation:mode==="regenerate"?"regenerate":"generate",model,status:"completed",input_tokens:Number(u.promptTokenCount||0)||null,output_tokens:Number(u.candidatesTokenCount||0)||null,total_tokens:Number(u.totalTokenCount||0)||null,completed_at:new Date().toISOString()});return NextResponse.json({ok:true,note:saved});}catch(e:any){return NextResponse.json({error:e?.message||"Could not generate note."},{status:500});}}